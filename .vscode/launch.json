{
  "configurations": [
    {
      "type": "aws-sam",
      "request": "direct-invoke",
      "name": "slack-autobot:SlackEventCallbackHandlerFunction",
      "invokeTarget": {
        "target": "template",
        "templatePath": "${workspaceFolder}/template.yaml",
        "logicalId": "SlackEventCallbackHandlerFunction"
      },
      "lambda": {
        "payload": {},
        "environmentVariables": {}
      }
    }
  ]
}
