export const connectOnboardingCall = `Description of the Connect onboarding call + Context for the model to underestand the call`;

export const connectKickoffCall = `Description of the Connect kickoff call + Context for the model to underestand the call`;

export const connectDiscoveryCall = `Description of the Connect discovery call + Context for the model to underestand the call`;

export const learnOnboardingCall = `Description of the Learn onboarding call + Context for the model to underestand the call`;

export const learnKickoffCall = `Description of the Learn Kickoff call + Context for the model to underestand the call`;

export const doceboSHO = `Description of the Docebo SHO call + Context for the model to underestand the call`;

export const trainicityTeamMeeting = `Description of the Trainicity team meeting call + Context for the model to underestand the call`;
